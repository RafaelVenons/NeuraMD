<% content_for :title, "Nova Nota — NeuraMD" %>

<div class="mx-auto max-w-lg">
  <div class="mb-6 flex items-center justify-between">
    <h1 class="text-2xl font-bold text-gray-900">Nova Nota</h1>
    <%= link_to "← Voltar", notes_path, class: "text-sm text-gray-500 hover:text-gray-700" %>
  </div>

  <%= render "form", note: @note %>
</div>
