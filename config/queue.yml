default: &default
  dispatchers:
    - polling_interval: 1
      batch_size: 500
  workers:
    - queues: "ai_remote"
      threads: <%= ENV.fetch("AI_REMOTE_JOB_THREADS", 1) %>
      processes: <%= ENV.fetch("AI_REMOTE_JOB_CONCURRENCY", 1) %>
      polling_interval: 0.1
    - queues: "*"
      threads: <%= ENV.fetch("JOB_THREADS", 3) %>
      processes: <%= ENV.fetch("JOB_CONCURRENCY", 1) %>
      polling_interval: 0.1

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
